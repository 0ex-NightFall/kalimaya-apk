# Kalimaya Mobile (Flutter)

UI: simple, modern, friendly untuk user gaptek.

## Setup
```
flutter create .
flutter pub get
flutter run
```

API base URL diatur via `--dart-define=API_URL=http://your-host:5000`.

## Bahasa
- Indonesia (default)
- English (toggle di profile / halaman login)
